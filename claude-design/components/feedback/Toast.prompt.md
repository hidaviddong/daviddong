Transient notification styled as a small floating frosted-glass panel (blurred translucent background, soft shadow, fully rounded corners) with a minimal round ✕ dismiss — the modern macOS notification-banner look, not a mini window.

```jsx
<Toast title="Saved" description="Your changes were saved." onClose={() => dismiss()} />
```
