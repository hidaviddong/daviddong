Boxed inline message, like a classic OS dialog banner. Uses a plain glyph (ℹ ✓ ! ✕) rather than an icon-font — keep it in the chrome font so it renders pixel-crisp.

```jsx
<Alert tone="warning" title="Low disk space">You have less than 10% free space remaining.</Alert>
```
