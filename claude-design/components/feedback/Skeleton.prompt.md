Loading placeholder — a plain rounded gray block that gently pulses opacity, the restrained modern-macOS convention (no diagonal stripes, no shimmer sweep). The pulse keyframes ship globally in `tokens/effects.css`.

```jsx
<Skeleton height={16} width={180} />
```
