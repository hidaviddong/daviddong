Rounded gloss meter — a pill-shaped sunken track filled by a smooth blue gradient bar that animates its width. No segments/blocks; Aqua progress bars are continuous.

```jsx
<Progress value={64} />
```
