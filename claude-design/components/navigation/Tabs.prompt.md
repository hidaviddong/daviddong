Segmented control — a rounded gray pill containing solid tab buttons, the active one lifted onto a white chip with a soft shadow. This is macOS's tab pattern (see System Preferences), not a file-folder tab strip.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[{value:"general", label:"General", content: <p>…</p>}]} />
```
