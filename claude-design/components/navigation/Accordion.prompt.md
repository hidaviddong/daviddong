Collapsible list — a rotating ▶ disclosure triangle exactly like Finder's list-view expand arrow, smoothly animated (Aqua eases, it doesn't snap).

```jsx
<Accordion items={[{title:"System Info", content: <p>…</p>}]} />
```
