Single-line text field with the classic sunken/inset bevel (reverse of a button's raised bevel).

```jsx
<Input placeholder="Type a command…" />
```

Always white/inset background regardless of surrounding panel color. Disabled state swaps to the gray panel face color instead of white.
