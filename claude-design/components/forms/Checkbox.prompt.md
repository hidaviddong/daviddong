Small rounded-square checkbox that fills solid Aqua blue with a white checkmark when checked — the modern macOS System Settings look, not a sunken box with an X.

```jsx
<Checkbox checked={agreed} onChange={(e) => setAgreed(e.target.checked)} label="Remember me" />
```
