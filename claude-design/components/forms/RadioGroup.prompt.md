Sunken circular radio buttons with an Aqua-blue filled dot when selected.

```jsx
<RadioGroup
  name="viewmode"
  value={viewMode}
  onChange={setViewMode}
  options={[{ value: "icons", label: "Icons" }, { value: "list", label: "List" }]}
/>
```
