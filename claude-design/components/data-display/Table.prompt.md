Data table — raised beveled header cells (like column-sort buttons in a classic file list view), sunken outer frame, subtle zebra striping.

```jsx
<Table columns={[{key:"name",label:"Name"},{key:"size",label:"Size"}]} rows={[{name:"readme.txt", size:"2KB"}]} />
```
