# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Cloudflare / Wrangler local state
.wrangler
.dev.vars

# Raw 3D source assets (large photogrammetry downloads) live outside public/
# so Vite never copies them into dist — only processed output ships.
raw-assets/
# Converted photogrammetry tiles (~4MB each × 20). Kept out of git while
# deciding between R2 hosting (like project images) and committing.
public/models/hk-tiles/
# Work-in-progress real-mesh export; still iterating on size before this
# goes to R2 (like project images) or gets committed.
public/models/causeway-bay-real.glb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
public
