---
import { navLinks } from "../config/nav";
---

<nav class="flex items-center gap-4">
    {navLinks.map((link) => 
        <a href={link.path} class="font-medium text-neutral-500 hover:text-neutral-900 hover:underline">
            {link.label}
    </a>)}
</nav>

